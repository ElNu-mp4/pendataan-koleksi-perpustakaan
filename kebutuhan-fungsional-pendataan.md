# 3.2.3 Kebutuhan Fungsional (Pendataan - Berdasarkan Kode Proyek)

Sub-bab ini merinci daftar fitur dan layanan spesifik yang wajib disediakan oleh sistem untuk mendukung aktivitas pengguna. Kebutuhan ini bersifat tegas dan dapat diuji (testable), berdasarkan eksplorasi mendalam kode proyek (Controller Pendataan.php, Administrator.php, views, dll.).

| No. | SRS ID | Deskripsi Kebutuhan | Kriteria Pengujian (Testable) |
|-----|--------|---------------------|------------------------------|
| 1 | SRS-F01 | Sistem harus menampilkan form survei pendataan koleksi dengan pertanyaan dinamis dari tabel `pertanyaan_koleksi` dan opsi dari `opsi_pertanyaan_koleksi`, berdasarkan status aktif di `config_pendataan`. | - Akses `/pendataan` menampilkan form dengan pertanyaan aktif (tipe: text, number, radio, checkbox, select, textarea). <br> - Jika status 'nonaktif', tampilkan pesan dari `pesan_nonaktif`. <br> - Query database mengambil data dari tabel terkait dengan cache TTL 86400 detik. |
| 2 | SRS-F02 | Sistem harus memvalidasi input survei dengan honeypot, rate limit per IP/session (maksimal 5 submit/jam), pencegahan duplikasi nomor_pokok per tahun, dan validasi field wajib/min/max. | - Submit dengan honeypot terisi redirect ke terima_kasih tanpa simpan. <br> - Submit ke-6 dalam 1 jam ditolak dengan flashdata error. <br> - Duplikasi nomor_pokok ditolak dengan pesan error. <br> - Field wajib kosong menampilkan validation_errors(). |
| 3 | SRS-F03 | Sistem harus menyimpan data survei dalam transaksi database: insert ke `identitas_input_koleksi` (dengan nomor_pokok awal '-', IP, session_id), lalu insert jawaban ke `jawaban_koleksi` dengan foreign key. | - Submit berhasil insert 1 record identitas dan N record jawaban (N = jumlah pertanyaan). <br> - Jika transaksi gagal, rollback dan tidak ada data tersimpan. <br> - Nomor_pokok diupdate jika diperlukan setelah insert. |
| 4 | SRS-F04 | Sistem harus membersihkan cache submission dan rate limit setelah commit berhasil, lalu redirect ke halaman terima_kasih. | - Cache submission dihapus; rate limit direset. <br> - Redirect ke `/pendataan/terima_kasih` dengan pesan sukses. <br> - Submit berikutnya tidak terpengaruh cache lama. |
| 5 | SRS-F05 | Sistem harus menyediakan halaman rekap (`/pendataan/rekap`) dengan filter tahun dari `identitas_input_koleksi`, nomor_pokok, tanggal range, dan per halaman (10-100). | - Dropdown tahun berisi DISTINCT tahun_data. <br> - Filter nomor_pokok dengan LIKE. <br> - Filter tanggal dengan DATE() >= dan <=. <br> - Per halaman berubah tanpa reload. |
| 6 | SRS-F06 | Sistem harus menampilkan AJAX chart stats (`get_chart_stats`) di halaman rekap dengan 5 grafik visual: Koleksi Saat Ini, Pertumbuhan Koleksi, Pengunjung & Akses Teknologi, Pemanfaatan Koleksi Tercetak, dan Distribusi Jenis Perpustakaan. | - Request AJAX mengembalikan JSON dengan `stat_cards`, `koleksi_saat_ini`, `pertumbuhan`, `pengunjung`, `pemanfaatan`, `distribusi_jenis`. <br> - Chart rendering menggunakan Chart.js 4.4.1 dari CDN. <br> - Halaman rekap menampilkan loading state, empty state, dan chart cards yang terbuka secara dinamis. <br> - Jika data kosong, tampilkan pesan chart kosong dan nilai statistik 0. |
| 7 | SRS-F07 | Sistem harus menyediakan tabel rekap dengan pagination, sorting (tanggal, nomor, jumlah_jawaban), dan total records, via AJAX `get_rekap_data`. | - Tabel menampilkan id_koleksi, nomor_pokok, tahun_data, tanggal_submit, jumlah_jawaban. <br> - Sorting ASC/DESC pada kolom yang dipilih. <br> - Pagination dengan offset dan limit, total dihitung. |
| 8 | SRS-F08 | Sistem harus menyediakan autentikasi admin dengan login username/password dari tabel `users`, dan cek akses modul 'pendataan' dari `users_modul`. | - Login benar redirect ke dashboard admin. <br> - Login salah tampilkan error. <br> - Akses tanpa session redirect ke login. <br> - Helper `cek_session_akses()` memverifikasi id_session dan id_modul. |
| 9 | SRS-F09 | Sistem harus menyediakan CRUD admin untuk data pendataan: list dengan filter tahun/nomor, tambah manual, edit, hapus, dan detail view. | - List menampilkan record dengan JOIN jawaban_koleksi untuk jumlah_jawaban. <br> - Tambah insert identitas dan jawaban secara manual. <br> - Edit update record existing. <br> - Hapus delete dengan konfirmasi. <br> - Detail tampilkan semua jawaban per pertanyaan. |
| 10 | SRS-F10 | Sistem harus mengizinkan admin mengkonfigurasi survei via `config_pendataan`: toggle status aktif/nonaktif, set tahun_aktif, dan pesan_nonaktif. | - Update status langsung berlaku di form publik. <br> - Tahun_aktif default ke date('Y') jika kosong. <br> - Pesan_nonaktif ditampilkan jika status 'nonaktif'. |
| 11 | SRS-F11 | Sistem harus menyediakan debug pendataan untuk admin dengan akses ke informasi teknis survei. | - Akses `/administrator/debug_pendataan` menampilkan info cache, rate limit, dll. <br> - Hanya admin dengan akses modul yang bisa masuk. <br> - Include file debug_pendataan.php dari APPPATH/views/administrator/. |
| 12 | SRS-F12 | Sistem harus mendukung ekspor data rekap ke Excel via POST form dengan filter yang sama seperti tabel. | - Button export mengirim POST ke `/pendataan/export_excel` dengan parameter filter. <br> - File Excel dihasilkan dengan data tabel yang difilter. <br> - Nama file berdasarkan tahun dan tanggal. |
| 13 | SRS-F13 | Sistem harus menampilkan halaman detail pendataan (`/pendataan/detail/{id_koleksi}`) dengan semua jawaban per pertanyaan, termasuk nama perpustakaan dari jawaban id_pertanyaan=2. | - URL dengan id_koleksi menampilkan detail record. <br> - Jawaban ditampilkan per section dan pertanyaan. <br> - Jika id_koleksi tidak ada, redirect ke rekap. |
| 14 | SRS-F14 | Sistem harus menggunakan caching untuk performa: questions (86400s), years (3600s), rekap_data (300s), detail (3600s), rate_limit (3600s), config (1800s). | - Data di-cache dengan key MD5 dari parameter. <br> - Cache diambil sebelum query database. <br> - Cache disimpan setelah query berhasil. |
| 15 | SRS-F15 | Sistem harus sanitasi input: XSS clean pada GET/POST, regex validasi tahun (YYYY), nomor_pokok (alphanumeric), tanggal (YYYY-MM-DD). | - Input GET/POST diproses dengan $this->security->xss_clean(). <br> - Tahun divalidasi dengan regex ^(19|20)\d{2}$. <br> - Tanggal divalidasi dengan regex ^\d{4}-\d{2}-\d{2}$. |
| 16 | SRS-F16 | Sistem harus menangani error dengan flashdata: error untuk validasi gagal, success untuk update config. | - Set_flashdata('error', message) redirect dengan alert danger. <br> - Set_flashdata('success', message) redirect dengan alert success. <br> - Alert dismissible dengan close button. |
| 17 | SRS-F17 | Sistem harus mendukung multi-section survei dengan pertanyaan dikelompokkan berdasarkan kolom 'section' di `pertanyaan_koleksi`. | - Form menampilkan section sebagai heading. <br> - Pertanyaan di-loop per section. <br> - Section names dari method get_section_names(). |
| 18 | SRS-F18 | Sistem harus mencegah akses tidak sah: AJAX hanya via is_ajax_request(), admin via cek_session_akses(), public tanpa login. | - Request non-AJAX ke get_chart_stats return JSON error 403. <br> - Admin tanpa akses modul redirect. <br> - Public dapat akses survei dan rekap tanpa login. |
| 19 | SRS-F19 | Sistem harus menyediakan navigasi menu admin untuk modul pendataan, dengan cek akses via umenu_akses("pendataan"). | - Menu admin menampilkan link "Pendataan Koleksi" jika akses granted. <br> - Link href ke base_url()."administrator/pendataan". <br> - Icon fa-circle-o. |
| 20 | SRS-F20 | Sistem harus menampilkan halaman terima_kasih setelah submit berhasil, dengan opsi kembali ke survei atau rekap. | - Redirect ke `/pendataan/terima_kasih` setelah commit. <br> - Halaman menampilkan pesan sukses dan tombol "Kembali ke Form" atau "Lihat Rekap". <br> - Tidak ada opsi isi ulang untuk mencegah duplikasi. |
| 21 | SRS-F21 | Sistem harus menyediakan halaman survei tertutup (nonaktif) dengan pesan khusus dari `config_pendataan`. | - Jika status='nonaktif', redirect ke `/pendataan/closed` dari index(). <br> - Halaman menampilkan pesan dari kolom `pesan_nonaktif`. <br> - User dapat kembali ke halaman utama. |
| 22 | SRS-F22 | Sistem harus menggunakan helper `_get_ip_address()` untuk mengidentifikasi pengguna berdasarkan IP address. | - IP diambil dari `$_SERVER['HTTP_X_FORWARDED_FOR']` atau `$_SERVER['REMOTE_ADDR']`. <br> - IP disimpan di field `ip_address` tabel `identitas_input_koleksi`. <br> - IP digunakan untuk rate limiting per IP. |
| 23 | SRS-F23 | Sistem harus menyediakan method helper untuk sanitasi input: `_sanitize_input()`, `_clean_input()`, dan `_sanitize_csv_value()`. | - `_sanitize_input()` recursively clean array dan string input. <br> - `_clean_input()` trim, xss clean, dan strip tags. <br> - `_sanitize_csv_value()` escape quote untuk CSV export. |
| 24 | SRS-F24 | Sistem harus menyediakan method helper untuk pengambilan pertanyaan dengan validasi: `_get_pertanyaan_for_validation()`. | - Query pertanyaan aktif dengan rules validasi (wajib, min/max). <br> - Return grouped by section untuk looping di form validation. <br> - Cache untuk performa. |
| 25 | SRS-F25 | Sistem harus menyediakan method helper untuk nama section survei: `get_section_names()`. | - Query DISTINCT `section` dari `pertanyaan_koleksi` aktif. <br> - Return array dengan key=section, value=section_name atau default section name. <br> - Gunakan untuk label di form dan detail view. |
| 26 | SRS-F26 | Sistem harus mencegah double submission dengan tracking session: `_check_double_submission()`, `_mark_submission()`. | - `_check_double_submission()` cek cache key 'double_submit_' + session_id. <br> - `_mark_submission()` set cache dengan TTL 300s setelah insert. <br> - Prevent rapid submit dalam window 5 menit. |
| 27 | SRS-F27 | Sistem harus menampilkan halaman tertutup dengan custom pesan jika survei nonaktif, via method `show_survey_closed()`. | - Method show_survey_closed() include view pendataan_closed.php. <br> - Pesan dimuat dari `config_pendataan.pesan_nonaktif`. <br> - HTML rendering dengan template standard. |
| 28 | SRS-F28 | Sistem harus mendukung bulk export: select checkbox di tabel, POST selected_ids ke `/pendataan/export_excel`. | - Tabel rekap menampilkan checkbox per row. <br> - POST selected_ids array saat export. <br> - Max 1000 items per export, query hanya selected IDs. |
| 29 | SRS-F29 | Sistem harus streaming export untuk handle unlimited records: chunk-based processing dengan limit memory 512M dan time 600s. | - Export proses data dalam chunk 50 records. <br> - Stream output dengan fputcsv() per chunk. <br> - ob_flush() dan flush() per iteration. <br> - UTF-8 BOM untuk Excel compatibility. |
| 30 | SRS-F30 | Sistem harus rate limit export: maksimal 1 export per 1 jam per session (key 'export_limit_session_'). | - Method `_check_export_rate_limit()` cek cache. <br> - Method `_update_export_rate_limit()` set cache TTL 3600s. <br> - Error message jika limit tercapai. |
| 31 | SRS-F31 | Sistem harus mendukung admin tambah pendataan manual: `/administrator/tambah_pendataan` dengan form yang sama seperti public. | - Form menampilkan semua pertanyaan aktif per section. <br> - Admin select tahun_data manual. <br> - Insert ke identitas dan jawaban dengan validasi sama seperti public submit. |
| 32 | SRS-F32 | Sistem harus mendukung admin edit pendataan: `/administrator/edit_pendataan/{id_koleksi}` dengan load existing jawaban. | - Load identitas dan jawaban existing. <br> - Form pre-fill dengan jawaban lama. <br> - Hapus jawaban lama, insert jawaban baru pada update. <br> - Update tahun_data jika berubah. |
| 33 | SRS-F33 | Sistem harus mendukung admin delete pendataan dengan cascading delete: hapus identitas dan jawaban terkait. | - DELETE FROM jawaban_koleksi WHERE id_koleksi = :id. <br> - DELETE FROM identitas_input_koleksi WHERE id_koleksi = :id. <br> - Return flashdata success/error. |
| 34 | SRS-F34 | Sistem harus menyediakan detail view untuk admin: `/administrator/detail_pendataan/{id_koleksi}` atau via modal view. | - Tampilkan identitas dan jawaban per section. <br> - Format jawaban berdasarkan tipe pertanyaan. <br> - Button untuk edit atau delete dari detail. |
| 35 | SRS-F35 | Sistem harus mendukung filter dan sorting di list admin pendataan: tahun, nomor_pokok, dengan order by tanggal_submit DESC. | - Filter dropdown tahun. <br> - Filter text nomor_pokok dengan LIKE. <br> - Display jumlah_jawaban dari COUNT(*) join. <br> - Default sort tanggal terbaru. |
| 36 | SRS-F36 | Sistem harus menyediakan debug page untuk admin: `/administrator/debug_pendataan` dengan info teknis cache, rate limit, dll. | - Akses via cek_session_akses('pendataan'). <br> - Include file debug_pendataan.php dari views/administrator/. <br> - Tampilkan info cache, rate limit counters, database stats. |
| 37 | SRS-F37 | Sistem harus menggunakan helper `cek_session_akses()` untuk verifikasi akses modul 'pendataan' di setiap admin endpoint. | - Function cek_session_akses('pendataan', $id_session) di application/helpers/. <br> - Redirect ke login atau error page jika akses ditolak. <br> - Cek tabel users_modul untuk permission. |
| 38 | SRS-F38 | Sistem harus menyediakan helper `umenu_akses()` untuk cek akses menu di view: `umenu_akses("pendataan")`. | - Function return TRUE jika user punya akses, FALSE jika tidak. <br> - Gunakan di sidebar/menu untuk show/hide link "Pendataan Koleksi". <br> - Query dari tabel users_modul. |
| 39 | SRS-F39 | Sistem harus menyediakan method helper di Admin untuk nama section: `get_pendataan_section_names()`. | - Return array dengan key=section, value=display name. <br> - Gunakan di form tambah/edit untuk section heading. <br> - Cocok dengan get_section_names() di Pendataan controller. |
| 40 | SRS-F40 | Sistem harus mendukung multi-language untuk pesan error dan sukses via flashdata. | - Flashdata 'error' untuk validasi gagal, rate limit, honeypot. <br> - Flashdata 'success' untuk config update, tambah, edit, delete. <br> - Display via alert danger/success di view template. |
| 41 | SRS-F41 | Sistem harus validate tahun input dengan regex `^(19|20)\d{2}$` di semua endpoint (filter, export, get_chart_stats). | - Tahun default ke date('Y') jika invalid. <br> - Prevent injection atau typo. <br> - Apply di GET/POST tahun parameter. |
| 42 | SRS-F42 | Sistem harus validate nomor_pokok dengan regex `^[a-zA-Z0-9\-\/\s]+$` untuk filter dan export. | - Strip karakter khusus dengan preg_replace. <br> - Prevent SQL injection atau XSS. <br> - Allow alphanumeric, dash, slash, space. |
| 43 | SRS-F43 | Sistem harus validate tanggal dengan regex `^\d{4}-\d{2}-\d{2}$` untuk date_from dan date_to di filter/export. | - Format YYYY-MM-DD. <br> - Default ke empty string '' jika invalid. <br> - Prevent injection. |
| 44 | SRS-F44 | Sistem harus mensupport jawaban tipe array (checkbox multi-select) dengan implode ke string ', ' saat saving. | - Input pertanyaan[id] dapat array jika checkbox. <br> - Implode dengan ', ' separator. <br> - Save sebagai string tunggal di jawaban_koleksi.jawaban. |
| 45 | SRS-F45 | Sistem harus limit jawaban max 1000 karakter untuk text dan textarea saat insert. | - Check strlen($jawaban) > 1000, substr jika diperlukan. <br> - Prevent overflow di database column. <br> - Apply di submit() dan tambah_pendataan(). |
| 46 | SRS-F46 | Sistem harus menggunakan database transaction untuk atomic insert identitas dan jawaban. | - $this->db->trans_begin(), trans_commit(), trans_rollback(). <br> - Rollback jika insert_id gagal atau trans_status FALSE. <br> - Ensure data integrity. |
| 47 | SRS-F47 | Sistem harus menampilkan form pendataan dengan pertanyaan yang dikelompokkan per section dari database. | - Query di index() loop by section. <br> - Section jadi heading (h4 atau legend). <br> - Pertanyaan di-loop per section. <br> - URutan by section, urutan column. |
| 48 | SRS-F48 | Sistem harus menampilkan notifikasi alert dengan closing button (×) untuk flashdata error/success. | - HTML dengan dismiss onclick. <br> - Alert danger untuk error (red). <br> - Alert success untuk success (green). <br> - Display di top rekap dan form pages. |
| 49 | SRS-F49 | Sistem harus support template rendering via `$this->template->load()` dengan template dinas-1. | - Load template dengan 3 param: folder, view name, data. <br> - Template dinas-1/template.php sebagai layout master. <br> - Integrate navigation, footer, CSS/JS. |
| 50 | SRS-F50 | Sistem harus load library dan helper di constructor: form, url, security, session, form_validation, cache driver file, database. | - Constructor Pendataan load helper dan library. <br> - Cache driver file-based untuk offline. <br> - Database connection auto-loaded via CI config. |